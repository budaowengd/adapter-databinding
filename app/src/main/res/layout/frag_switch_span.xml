<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="model"
            type="me.lx.sample.model.SwitchSpanModel" />

        <variable
            name="click"
            type="me.lx.sample.ClickListeners" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cbind3_test="@{3}"
        app:bbind2_test="@{2}"
        app:abind1_test="@{1}"
        android:orientation="vertical">

        <Spinner
            android:id="@+id/spinnerWidth"
            android:layout_width="120dp"
            android:layout_height="47dp"
            android:dropDownWidth="120dp"
            android:entries="@array/print_width_type"
            android:spinnerMode="dialog"
            android:textSize="16sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/toolbar" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            tools:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:rv_layout_span="@{model.layout}"
            app:rv_adapter="@{model.adapter}"
            app:rv_itemBinding="@{model.getXmlItem()}"
            app:rv_items="@{model.singleItems}"
             />
<!-- app:rv_layoutmanager="@{(v) -> model.getLm(v)}"  -->
        <LinearLayout
            style="?buttonBarStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#3443">

            <Button
                android:id="@+id/add"
                style="?buttonBarButtonStyle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:onClickListener="@{() -> click.clickAddItem()}"
                android:text="Add Item" />

            <Button
                android:id="@+id/remove"
                style="?buttonBarButtonStyle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:onClickListener="@{() -> click.clickRemoveItem()}"
                android:text="Remove Item" />
        </LinearLayout>
    </LinearLayout>
</layout>
