<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>

        <variable
            name="headerClick"
            type="me.lx.rv.group.BaseClickGroupListener" />

        <variable
            name="headerItem"
            type="me.lx.sample.group.entity.GroupEntity" />

    </data>
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{()->headerClick.clickGroupItem(headerItem)}"
        android:background="@drawable/holo_red_dark_ripple_bg"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="10dp"
            android:text="@{headerItem.header}"
            android:textColor="@android:color/white"
            android:textSize="20sp" />

    </LinearLayout>
</layout>