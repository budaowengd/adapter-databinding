<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>

        <variable
            name="gFooterClick"
            type="me.lx.rv.group.BaseFun1ClickGroupListener" />

        <variable
            name="gFooter"
            type="me.lx.sample.group.entity.GroupEntity" />

    </data>
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{()->gFooterClick.clickGroup(gFooter)}"
        android:background="@drawable/holo_orange_light_ripple_bg"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_footer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="10dp"
            android:text="@{gFooter.footer}"
            android:textColor="@android:color/white"
            android:textSize="20sp" />

    </LinearLayout>
</layout>